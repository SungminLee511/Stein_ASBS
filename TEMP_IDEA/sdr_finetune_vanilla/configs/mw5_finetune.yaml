# @package _global_
#
# MW5: Fine-tune SDR checkpoint with sdr_lambda=0 (vanilla ASBS loss)
# Usage: python train.py --config-path=TEMP_IDEA/sdr_finetune_vanilla/configs \
#            --config-name=mw5_finetune \
#            checkpoint=<path_to_sdr_checkpoint>

defaults:
  - /problem: mw5
  - /source: gauss
  - /sde@ref_sde: ve
  - /model@controller: fouriermlp
  - /state_cost: zero
  - /term_cost: score_term_cost
  - /matcher@adjoint_matcher: sdr_adjoint_ve

exp_name: mw5_finetune

# === source ===
scale: 2

# === sde (same as SDR training) ===
nfe: 200
sigma_max: 3
sigma_min: 0.001
rescale_t: null

# === SDR parameters: lambda=0 disables KSD correction ===
sdr_lambda: 0.0
sdr_beta: 0.0

# === training ===
num_epochs: 5500      # SDR trained 5000, fine-tune 500 more
clip_grad_norm: 1.0
max_grad_E_norm: 50

adjoint_matcher:
  buffer_size: 10000
  duplicates: 10
  resample_size: 1000
  num_epochs_per_stage: ${num_epochs}
  optim:
    lr: 5e-4          # half of original 1e-3 for stability
    weight_decay: 0

# === eval & logging ===
use_wandb: false
skip_eval: true
eval_freq: 200
save_freq: 50         # frequent saves to eval externally
