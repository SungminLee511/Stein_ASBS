# @package _global_

shared_dir: /home/RESEARCH/Stein_ASBS/results
hydra:
  run:
    dir: ./results/${exp_name}/seed_${seed}
  sweep:
    dir: ./results/${now:%Y.%m.%d}/${now:%H%M%S}
    subdir: ${hydra.job.num}
