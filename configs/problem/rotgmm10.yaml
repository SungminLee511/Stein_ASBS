# @package _global_

dim: 10
n_particles: 1
spatial_dim: 10

energy:
  _target_: adjoint_samplers.energies.rotated_gmm_energy.RotatedGMMEnergy
  dim: ${dim}
  n_modes: 8
  mode_sep: 5.0
  mode_std: 0.5

evaluator:
  _target_: adjoint_samplers.components.generic_evaluator.RotatedGMMEvaluator
