# @package _global_

dim: 66
n_particles: 22
spatial_dim: 3

energy:
  _target_: adjoint_samplers.energies.aldp_energy.AlanineDipeptideEnergy
  dim: ${dim}
  temperature: 300.0
  platform: CPU
  max_energy: 1000.0

# ALDP evaluator requires reference data — set skip_eval: true until available
evaluator: null
