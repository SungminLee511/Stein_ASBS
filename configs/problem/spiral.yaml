# @package _global_

dim: 2
n_particles: 1
spatial_dim: 2

energy:
  _target_: adjoint_samplers.energies.viz_energies.SpiralEnergy
  dim: ${dim}
  n_turns: 2.0
  width: 0.15

evaluator:
  _target_: adjoint_samplers.components.generic_evaluator.GenericEnergyEvaluator
